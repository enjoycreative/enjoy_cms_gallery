class <%= camelcased_class_name %> < Enjoy::Gallery::EmbeddedImage
  embedded_in :<%= underscored_parent_class_name %>, class_name: "<%= camelcased_parent_class_name %>"

  def self.find(id)
    find_through(<%= camelcased_parent_class_name %>, '<%= underscored_pluralized_class_name %>', id)
  end

  def image_styles
    {}
  end

  def image_jcrop_options
    {}
  end


  ############# rails_admin ##############
  def self.rails_admin_add_fields
    {}
  end

  def self.rails_admin_add_config(config)
  end

  rails_admin(&Enjoy::Gallery::Admin::EmbeddedImage.config(nil, rails_admin_add_fields) { |config|
    rails_admin_add_config(config)
  })

end
